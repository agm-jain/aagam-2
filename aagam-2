#include <stdio.h>
int main() {
    float n;
    printf("Enter an integer: ");
    scanf("%d", &n);

    
    if(num % 2 == 0)
        printf("%d is even.", n);
    else
        printf("%d is odd.", n);
    
    return 0;
}
